{
    "version" : "2018-05-29",
	"operation" : "Query",
	"query" : {
		"expression": "company = :company and project_id = :project_id",
        "expressionValues" : {
            ":company" : $util.dynamodb.toDynamoDBJson($ctx.args.company),
            ":project_id" : $util.dynamodb.toDynamoDBJson($ctx.args.project_id)
        }
	},
	"index": "company_index"
	#if($ctx.args.nextToken)
			,"nextToken": $util.toJson($ctx.args.nextToken)
	#end
}